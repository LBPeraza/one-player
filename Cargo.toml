[package]
name = "one-player"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["2d", "audio", "ui"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
